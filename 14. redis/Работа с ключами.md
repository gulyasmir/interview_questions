```
await redis.set('name', 'Alice');  // Установить значение
const value = await redis.get('name');  // Получить значение
console.log(value); // "Alice"

await redis.del('name');  // Удалить ключ
await redis.expire('name', 60);  // Установить TTL (в секундах)

```
